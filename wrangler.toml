name = "courtboard"
compatibility_date = "2024-09-23"
pages_build_output_dir = "public"

# After creating a KV namespace (see README), put its id here so
# `wrangler pages deploy` binds it automatically. Not required if you
# bind it through the Cloudflare dashboard instead.
# [[kv_namespaces]]
# binding = "SCORES_KV"
# id = "your-kv-namespace-id"
